# TradeClaw Daily Standup — 2026-06-26

- Run: scheduled daily loop, 09:35 GMT+8 (unattended).
- Branch: `loop/standup-2026-06-26` (created from `main`; `main` is protected, so work is superseded via branch + PR, never committed to main).
- HEAD at run start: `ab59ce01`.
- Ground truth: local `main` is **diverged** — 2 ahead / 17 behind `origin/main`. Local has 2 unpushed chore commits; `origin/main` has 17 commits (merged PRs) local lacks. No stashes, no in-progress merge/rebase, single worktree.

## DONE (last 24h)

Two commits, both on the strategy-audit-agent lane (these are the 2 unpushed-to-origin commits):

- `ab59ce01` chore(strategy-audit-agent): add patch bundle source, tests, and audit docs
- `03e56c7c` chore: ignore graphify-out and strategy-audit-agent tmp output

No product/runtime code, migrations, or deploys in the last 24h window on this local branch.

## PENDING (not finished)

Working-tree drift — a **docs-only "AI improvement" checkpoint** (CEO Zaky recurring agent lane), not this loop's work. Left uncommitted by design; this standup does not bundle it:

- `STATE.yaml` — timestamp bump only (2026-06-08 → 2026-06-26).
- `docs/ai-improvement/README.md`, `implementation-log.md`, `uncommitted-source-verification-handoff.md`, `verification-command-matrix.md` — checkpoint refresh narrowing the dirty tree to a single intentional lane.
- `scripts/research/recost-segment.ts` (untracked) — read-only research CLI. Re-costs the track record at the **real per-row cost** (`cost_estimate_pct`, migration 051: ~0.40% crypto / ~0.10% metals / ~0.04% FX) instead of the live equity curve's flat 0.02%, which undercharges crypto ~20×. It mutates nothing, reads only `signal_history`, typechecks clean, and fails closed without `DATABASE_PUBLIC_URL`/`DATABASE_URL`. Note: this is a LOCAL/dev research probe, not part of the production verified record.

Open decisions / drift:

- **Keep-or-drop** `scripts/research/recost-segment.ts` — the AI-improvement docs flag this as the lone intentional dirty path awaiting a keep (add discoverability + track) vs drop decision.
- **2 unpushed commits** on local `main` not on `origin/main`.
- **Branch divergence**: local `main` is 17 commits behind `origin/main` (merged PRs incl. #122 PRNG-stat removal, #123 referrals, #124 checkout-intent auth, #125 PostHog funnel, #126 hero A/B, #127 premium UI pass, #134 strategy-audit package). Local main needs reconciling with origin.

## NEXT

1. **Reconcile local `main` with `origin/main`** (human call, top ops item): rebase/replay the 2 local chore commits onto `origin/main`, or confirm they are superseded by merged PR #134, so local stops diverging. Do not force-recover; supersede via branch + PR.
2. **Resolve `recost-segment.ts` keep/drop.** If kept: add a discoverability hook/docs entry, then run it against Railway Postgres (`railway run -- npx tsx scripts/research/recost-segment.ts`) to answer the real-cost edge question — does any asset-class × band subset clear its own cost? This directly tests the standing "engine has no net edge" finding (radical-transparency contract: report the answer whether it clears or not).
3. **Close out `docs/plans/2026-06-14-track-record-honesty-fixes.md`**: verify the 3 layered commits (api `summaryOnly`, `classifyBandComparison`, realized-compounded-return headline) actually landed on `origin/main`; if so, mark the plan done; if not, schedule the remaining layer.

## graphify highlights

Source: seeded `graphify-out/GRAPH_REPORT.md` extract (2026-06-24; 8606 nodes · 15270 edges · 609 communities). Incremental `--update` was deferred this run: under the loop's narrow Bash allowlist `GEMINI_API_KEY` presence is unverifiable, and a timeout-killed extract could corrupt the seeded graph — the contract says never fail the run over graphify. Changed files since the extract are docs + one research script, which do not move the God-Node/cycle picture.

- **God Nodes (core abstractions):** `readSessionFromRequest()` (114 edges — by far the most central; auth/session is the spine), `execute()` (62), `query()` (60), `getUserById()` (49), `requireCronAuth()` (46), `getTrackedSignals()` (42), `queryOne()` (41), `getTrackedSignalsForRequest()` (34), `getUserTier()` (33), `getOHLCV()` (32).
- **Surprising Connections:** backtest `ComparisonChartProps` / `MetricsTableProps` → `BacktestResult` (UI coupled directly to `packages/strategies/src/run-backtest.ts`); `refreshAtrCalibration()` → `calibrateAtrMultiplier()`; `generateSignalsFromTA()` and `buildHistoricalSignal()` both → `getStrategyName()` (signal-id path leaning on `packages/signals/src/types.ts`).
- **Import Cycles (coupling to watch):** 3× in `packages/agent/src/channels/base.ts` ↔ `{telegram,webhook,discord}.ts`; 1× in `apps/web/lib/alert-channels.ts` ↔ `apps/web/lib/email-sender.ts`. All 2-file cycles — extract the shared contract into a third module if either side is touched.

## Sync result

- Mode: **PR, never direct-to-main.** `main` is protected and diverged, so this standup is committed on `loop/standup-2026-06-26` and integrated via PR to `main` for human review (which also surfaces the 2 stranded local commits + the divergence).
- Staged by explicit path: `docs/automation/standups/2026-06-26.md` only. No `git add -A`.
- Final commit SHA and PR URL are recorded in this run's report and `loops/state/tradeclaw.STATE.md` (out-of-repo) to avoid amending/force-pushing the doc commit.
- No product code, migrations, deploys, secret writes, or Stripe/Postgres mutations in this run.
