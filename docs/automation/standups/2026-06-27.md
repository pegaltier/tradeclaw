# TradeClaw Daily Standup — 2026-06-27

Branch: `loop/standup-2026-06-26` · HEAD `f68994f2` · upstream in sync (0 ahead / 0 behind)
Scheduled unattended run, 09:30 GMT+8. Status + safe sync only — no product code, no migrations, no deploys.

## Ground truth
- Loop branch is NOT protected — safe to commit. Two sibling loop worktrees live (`btc-daily-sleeve`, `real-cost-track-record`) — left untouched.
- Working tree drift is from a **different** concurrent agent (CEO Zaky improvement agent): its docs cite `## main...origin/main [ahead 2, behind 17]`, i.e. authored against `main`, not this loop branch. Documented concurrent-session hazard — not committed here.
- No stashes, no in-progress merge/rebase.

## DONE (last ~24h)
Branch-local: only `f68994f2` (yesterday's standup). Real shipped outcomes landed on `main` via merged PRs:
- **#137 MERGED** — killed the BTC daily-momentum sleeve; fails fresh reproduction. Confirms the standing finding: single-asset timing has no net edge after real costs.
- **#136 MERGED** — public equity curve now charges the **real per-row execution cost** instead of a flat 0.02%. The flat constant undercharged crypto ~20x, so the prior published curve was the optimistic version. Radical-transparency fix: the headline curve now tells the truth.
- Older context still relevant: #121 (premium badge truthfulness + realized return at headline), #120 (resolved-signal denominator on public OG/social).

## PENDING (not finished)
- **Zaky doc drift (uncommitted, authored against `main`)** — `STATE.yaml` + `docs/ai-improvement/{README,implementation-log,uncommitted-source-verification-handoff,verification-command-matrix}.md`. A docs-only checkpoint refresh describing the new research script. Belongs on a Zaky branch, not this standup PR — reported, not captured.
- **`scripts/research/recost-segment.ts` (untracked, read-only)** — re-costs the track record at the REAL per-row cost (`cost_estimate_pct`, migration 051: ~0.40% crypto / ~0.10% metals / ~0.04% FX) and segments net expectancy by asset class × band. Typechecks clean; fails closed without DB env. Keep-vs-drop decision unresolved.
- **The probe is unanswered.** It needs a Postgres connection the local checkout does not have — so the only question that matters (does ANY asset-class × band subset clear its own real cost?) is still pending real production data. LOCAL scaffolding only; no production-verified result yet.
- **Branch is behind `main`** — the #136/#137 merges are not integrated into this loop branch. Expected for a docs branch.
- **PR #135** (this standup branch) still OPEN.
- Branch name `loop/standup-2026-06-26` is one day stale for a 2026-06-27 run (launcher-supplied; left as-is).

## NEXT
1. Decide keep/drop on `recost-segment.ts`. If kept: track it and run against Railway Postgres (`railway run -- npx tsx scripts/research/recost-segment.ts`) to get the cost-reality answer — this is the live edge question; label any output PROD-verified vs LOCAL.
2. Reconcile the Zaky doc-drift onto its own branch/PR so it does not ride standup PRs (one concern per commit).
3. Land the standup commit; update PR #135.

## graphify highlights (seeded report 2026-06-24; not re-run this cycle — only one untracked script changed since, no graph-relevant code shift)
- **God Nodes (core abstractions):** `readSessionFromRequest()` 114 edges · `execute()` 62 · `query()` 60 · `getUserById()` 49 · `requireCronAuth()` 46 · `getTrackedSignals()` 42 · `queryOne()` 41 · `getTrackedSignalsForRequest()` 34 · `getUserTier()` 33 · `getOHLCV()` 32. Auth/session + DB query + signal-tracking are the load-bearing hubs — change them carefully.
- **Surprising connections:** backtest UI (`comparison-chart.tsx`, `metrics-table.tsx`) reaches straight into `packages/strategies/src/run-backtest.ts:BacktestResult`; `signal-generator.ts` → `packages/signals/src/types.ts:getStrategyName()` (inferred).
- **Import cycles (4):** `packages/agent/src/channels/base.ts` ↔ `{telegram,webhook,discord}.ts` (3 cycles via the channel base) and `apps/web/lib/alert-channels.ts` ↔ `email-sender.ts`. Coupling worth watching if the agent channel layer is refactored.

## Sync result
- Committed `docs/automation/standups/2026-06-27.md` only (matches established standup pattern — yesterday's commit touched 1 file) on `loop/standup-2026-06-26`, by explicit path.
- Did NOT commit the Zaky doc drift or the untracked research script — different agent's in-flight work, authored against `main`.
- Pushed; PR #135 updated.
</content>
