# TradeClaw Daily Standup — 2026-06-28

Branch: `loop/standup-2026-06-26` · HEAD `9fd02301` · upstream in sync after catch-up (0 ahead / 0 behind `origin/loop/standup-2026-06-26`)
Scheduled unattended run, 09:30 GMT+8. Status + safe sync only — no product code, no migrations, no deploys.

## Ground truth
- Loop branch is NOT protected — safe to commit. Three sibling worktrees live (`.claude/worktrees/btc-daily-sleeve`, `C:/Ai/tradeclaw-newsfix` on `fix/news-mock-no-fake-signals`) — left untouched.
- **Concurrent-writer event at run start:** local HEAD was `435ac03c`, **8 commits BEHIND** `origin/loop/standup-2026-06-26` (remote tip `9fd02301`). A concurrent **strategy-audit lane** pushed 8 commits onto this same branch since yesterday's run. Caught up with a verified-safe `git merge --ff-only` (the 8 commits touch only `.gitignore` + `packages/strategy-audit-agent/*`, zero overlap with the dirty working tree). New HEAD `9fd02301`, working-tree drift intact.
- Working-tree drift is a **different** concurrent agent (CEO Zaky AI-improvement lane): its docs cite `## main...origin/main [ahead 2, behind 17]`, i.e. authored against `main`, not this loop branch. `STATE.yaml` change is a timestamp bump only (2026-06-08 → 2026-06-26). Not committed here.
- No stashes, no in-progress merge/rebase.

## DONE (last ~24h)
Eight `strategy-audit-agent` commits integrated from origin (concurrent lane, range `435ac03c..9fd02301`) — all **tooling-package** work under `packages/strategy-audit-agent`, no core-app product/runtime code, no migrations, no deploys:
- **Audit host-allowlist hardening** — `f93db1bd` include the `www` host in the default allowlist; `8e7cb83b` allow allowlist entries that carry a port. The audit runner now accepts the real production hostnames it was rejecting.
- **Audit correctness** — `1469bb41` reject duplicate run ids (idempotency guard); `5f4e8f85` parse signal trade counts from extracted output.
- **Audit runner robustness** — `7bee6c06` harden the deterministic browser runner.
- **Audit preset fidelity** — `45b4d19e` target the real preset checkboxes; `9fd02301` use real preset names in the sample request, so the audit drives the actual UI presets instead of placeholders.
- **Repo hygiene** — `89e45b5f` ignore root audit output.

Branch-local standup carry-over: `435ac03c` (yesterday's 2026-06-27 standup). Real product outcomes (#136 real-cost equity curve, #137 BTC sleeve killed) remain on `main` via the merges reported yesterday — not re-counted here.

## PENDING (not finished)
- **PR #135 is CONFLICTING / DIRTY against `main`** (`mergeable: CONFLICTING`, `mergeStateStatus: DIRTY`). The branch is now **12 ahead / 18 behind `main`** (`origin/main` at `1fe4cc38`); main moved under it with merged PRs (incl. #136/#137). This standup PR can no longer fast-forward or auto-merge. Needs human reconciliation — NOT resolved here (conflict resolution is product reconciliation, out of standup scope, and unsafe to attempt while concurrent lanes push to this branch).
- **Zaky doc drift (uncommitted, authored against `main`)** — `STATE.yaml` (timestamp bump) + `docs/ai-improvement/{README,implementation-log,uncommitted-source-verification-handoff,verification-command-matrix}.md`. A docs-only AI-improvement checkpoint. Belongs on a Zaky branch, not this standup PR — reported, not captured.
- **`scripts/research/recost-segment.ts` (untracked, read-only — verified)** — confirmed by source read: a single `SELECT` against `signal_history`, mutates nothing, and **fails closed** via `process.exit(1)` when neither `DATABASE_PUBLIC_URL` nor `DATABASE_URL` is set (the only write is an optional `--json` local file). It re-costs the track record at the REAL per-row cost (`cost_estimate_pct`, migration 051: ~0.40% crypto / ~0.10% metals / ~0.04% FX) instead of the live equity curve's flat 0.02% — that flat constant undercharges crypto ~20×, so the published equity curve (≈ −42.5%, per the script's cited source) is the **optimistic** version. Keep-vs-drop decision still unresolved.
- **The probe is unanswered.** Its core question — does ANY asset-class × band subset clear its own real cost? — needs a production Postgres connection the local checkout does not have. LOCAL scaffolding only; **no production-verified result**. Still pending real data.
- **Stale branch name** — `loop/standup-2026-06-26` is now 2 days old for a 2026-06-28 run (launcher-supplied; left as-is).

## NEXT
1. **Unblock PR #135.** It is CONFLICTING against `main`. Human call: rebase this branch onto `origin/main`, or land standups via a fresh branch cut from current `main` so they stop accumulating conflicts. Do not force-recover.
2. **Decide keep/drop on `recost-segment.ts`.** If kept: track it by explicit path and run it against Railway Postgres (`railway run -- npx tsx scripts/research/recost-segment.ts`) to get the real-cost edge answer — this is the live question; label any output PROD-verified vs LOCAL, and report it whether or not any subset clears its cost (radical-transparency contract).
3. **Move the Zaky doc-drift onto its own branch/PR** so it stops shadowing standup PRs (one concern per commit).
4. **Land today's standup commit; update PR #135.**

## graphify highlights (seed report 2026-06-24; `--update` deferred — reasoning below)
- **God Nodes (core abstractions, by edge count):** `readSessionFromRequest()` 114 · `execute()` 62 · `query()` 60 · `getUserById()` 49 · `requireCronAuth()` 46 · `getTrackedSignals()` 42 · `queryOne()` 41 · `getTrackedSignalsForRequest()` 34 · `getUserTier()` 33 · `getOHLCV()` 32. Auth/session + DB query + signal-tracking remain the load-bearing hubs — change them carefully.
- **Surprising connections:** backtest UI (`comparison-chart.tsx`, `metrics-table.tsx`) references `packages/strategies/src/run-backtest.ts:BacktestResult` directly; `signal-generator.ts` and `signal/[id]/page.tsx` both call `packages/signals/src/types.ts:getStrategyName()` (inferred); `atr-calibration-cache.ts` → `calibrateAtrMultiplier()` (inferred).
- **Import cycles (4):** `packages/agent/src/channels/base.ts` ↔ `{telegram,webhook,discord}.ts` (3 cycles via the channel base) and `apps/web/lib/alert-channels.ts` ↔ `email-sender.ts`. Coupling to watch if the agent channel layer is refactored.
- **Re-run judgment — defer.** Since the 2026-06-24 seed the only changes are the 8 `strategy-audit-agent` `.mjs` tooling commits + docs + one untracked research script — none touch the core-app God Nodes (auth/session, DB query, signal-tracking), so the God-Node/cycle picture is unchanged. `GEMINI_API_KEY` presence is unverifiable under the loop's guarded shell (env expansion is blocked), and a timeout-killed `--update` could corrupt the seeded graph; the contract says never fail the run over graphify.

## Sync result
- **Catch-up:** run started 8 behind `origin/loop/standup-2026-06-26` (concurrent strategy-audit lane). `git merge --ff-only` to `9fd02301`, verified non-overlapping with the dirty tree. Now 0 ahead / 0 behind origin.
- Committed `docs/automation/standups/2026-06-28.md` only, by explicit path, on `loop/standup-2026-06-26`. No `git add -A`.
- Did NOT commit the Zaky doc drift or the untracked research script — different agents' in-flight work, authored against `main`.
- Pushed; PR #135 updated. **Flagged:** PR #135 is CONFLICTING against `main` and needs human reconciliation — not resolved in this run.
- No product code, migrations, deploys, secret writes, or Stripe/Postgres mutations in this run.
